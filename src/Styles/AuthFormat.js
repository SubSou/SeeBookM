import { StyleSheet } from 'react-native';

export const AuthFormat = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
    height: '100%',
    alignItems: 'center',
    paddingHorizontal: 30,
  },
});
